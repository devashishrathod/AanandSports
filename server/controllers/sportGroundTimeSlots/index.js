const { createRange } = require("./createRange");
const { getAll } = require("./getAll");
const { update } = require("./update");
const { deleteTimeSlot } = require("./delete");

module.exports = { createRange, getAll, update, deleteTimeSlot };
